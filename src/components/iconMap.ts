import { ShieldCheck, MessageCircle, Gauge, PenTool, Globe, Database, AppWindow, Sparkles, LucideIcon } from 'lucide-react'

export const iconMap: Record<string, LucideIcon> = {
  ShieldCheck,
  MessageCircle,
  Gauge,
  PenTool,
  Globe,
  Database,
  AppWindow,
  Sparkles,
}
